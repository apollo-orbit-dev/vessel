/// <reference types="vite/client" />

// Injected by Vite `define` from host/package.json (see vite.config.ts) — the
// single source of truth for the displayed host version (Phase 13).
declare const __APP_VERSION__: string;
